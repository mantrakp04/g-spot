{
  "name": "desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "turbo -F web build && electrobun dev",
    "dev:hmr": "concurrently \"bun run hmr\" \"bun run start\"",
    "hmr": "turbo -F web dev",
    "build": "turbo -F web build && electrobun build",
    "build:stable": "turbo -F web build && electrobun build --env=stable",
    "build:nightly": "turbo -F web build && electrobun build --env=canary",
    "build:canary": "turbo -F web build && electrobun build --env=canary",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@g-spot/db": "workspace:*",
    "@g-spot/types": "workspace:*",
    "electrobun": "catalog:",
    "server": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/three": "^0.182.0",
    "concurrently": "^9.2.1",
    "typescript": "^6"
  }
}
