{
  "name": "gmail-relay",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "check-types": "tsc --noEmit",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@g-spot/chat-state-sqlite": "workspace:*",
    "@g-spot/env": "workspace:*",
    "@stackframe/react": "^2.8.78",
    "chat": "^4.26.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@g-spot/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "^6"
  }
}
