{
  "name": "@g-spot/relay-db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./schema": {
      "default": "./src/schema/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "db:check": "RELAY_DRIZZLE_ONLY=1 drizzle-kit check",
    "db:push": "RELAY_DRIZZLE_ONLY=1 drizzle-kit push",
    "db:generate": "RELAY_DRIZZLE_ONLY=1 drizzle-kit generate",
    "db:migrate": "RELAY_DRIZZLE_ONLY=1 drizzle-kit migrate"
  },
  "dependencies": {
    "@g-spot/env": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@g-spot/config": "workspace:*",
    "drizzle-kit": "^0.31.10",
    "typescript": "^6"
  }
}
